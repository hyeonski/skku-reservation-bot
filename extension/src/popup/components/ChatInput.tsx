/**
 * 채팅 입력창.
 *
 * TODO:
 * - props: onSubmit: (text: string) => void, disabled?: boolean
 * - Enter로 전송, Shift+Enter는 줄바꿈
 */

export function ChatInput(_props: { onSubmit: (text: string) => void; disabled?: boolean }) {
  // TODO
  return <form className="chat-input" />;
}
